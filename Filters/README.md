# Filters

Stock filters are in `CanvasManagement.Filters/` (one assembly). Extra filter projects can sit beside it; `deploy.ps1` publishes every `Filters/**/*.csproj` into `deploy/Filters/`.

**How to write one:** [docs/FILTERS.md](../docs/FILTERS.md)
