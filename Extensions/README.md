# Extensions

Each subdirectory is a plugin. The host loads `Extensions/**/*.dll` next to `verpixeld.dll`.

**How to write one:** [docs/EXTENSIONS.md](../docs/EXTENSIONS.md)

`deploy.ps1` (repo root) publishes every `*.csproj` in this folder.
